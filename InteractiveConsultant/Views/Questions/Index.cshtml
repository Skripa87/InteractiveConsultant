@model IEnumerable<InteractiveConsultant.Models.Question>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TextQuestion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TextQuestion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDQuestion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDQuestion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDQuestion })
        </td>
    </tr>
}

</table>
